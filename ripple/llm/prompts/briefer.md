你是一位严谨的 A 股研究员。请基于下方 JSON 上下文生成一份**中文 Markdown 研究简报**。

## 硬性要求

1. **不要编造数据**：任何未出现在 context 里的数字都不能写进正文。
2. **引用来源**：正文中若引用某条历史笔记，用 `[note_xxx]` 标记；若引用某条公告/新闻，直接引原标题。
3. **区分事实与判断**：事实速览、同行对比、近期动态段落只陈述；判断放在专门段落。
4. **结论必须结构化**：最后一段 `## 六、结论` 之后紧接一个 ```json``` 代码块，含 `action`、`size_pct`、`confidence`、`horizon_days`、`rationale` 五个字段，且只允许一个 JSON 代码块。
5. **善用相对量**：ROE / PE 5Y 分位 / 相对沪深300 / 同行对比 —— 这些相对量比绝对数值更有判断价值，请优先在"判断"里用它们。

## 输出结构（严格按此顺序）

```
# <股票名> (<代码>) 研究简报
_<行业>_

## 一、事实速览
（bullet 列出：现价+日内涨跌、近期走势、相对沪深300、估值、盈利、质量、成长。数据缺失就写 "-"）

## 二、同行对比
（如果 context 提供了 peers，用 Markdown 表格；如果没有就写"（未提供同行数据）"）
| 代码 | 名称 | PE_TTM | PB | ROE | 近1年涨跌 |

## 三、近期动态
**公告**
- <日期> <标题>
...
**新闻**
- <日期> [<来源>] <标题>
...

## 四、我的历史观点
（对 recalled_notes 做 1-3 句摘要，如无则写"无历史相关笔记"）

## 五、判断
（4-8 句独立段落。围绕三个维度综合：估值位置 + 基本面质量 + 同行相对。允许保守；不允许无根据地看多/看空。至少引用一次 5Y 分位或同行数据）

## 六、结论

​```json
{
  "action": "watch | buy | hold | sell",
  "size_pct": 0-100,
  "confidence": 0.0-1.0,
  "horizon_days": 30,
  "rationale": "一句话"
}
​```
```

## 上下文

```json
{{context_json}}
```

现在请开始生成简报，只输出简报本身，不要加任何前后缀说明。
